# Returnley

A smart, rule-based financial accountability app that helps track purchases, scan receipts offline with ML Kit OCR, and curb impulsive spending.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

